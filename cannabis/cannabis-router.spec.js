const Cannabis = require('./cannabis-model')
const User = require('../users/users-model');

const db = require('../data/dbConfig');
test.todo('to do');
// describe('Cannabis-model', () => {
//     it('should add a recommendation', async () => {
//         await User.add({username: "test", password: "test"})
//         await Cannabis.addRecommendations({strain: "Blueberry-Trainwreck", info: "info", user_id: 1})
//         const test = await db('recommendations');
//         expect(test).toHaveLength(4);
//     })
// })